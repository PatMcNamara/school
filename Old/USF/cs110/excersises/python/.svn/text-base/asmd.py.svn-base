#Prompts user for numbers: no inputs, returns the inputed number, no side-effects
def getNumbers():
	number = input("Enter Number")
	return number

#adds 2 numbers: input 2 numbers, returns sum of numbers, no side-effects
def add(number1, number2):
	return number1 + number2

#subtract 2 numbers: input 2 numbers, returns differance of numbers, no side-effects
def subtract(number1, number2):
	return number1 - number2

#multiply 2 numbers: input 2 numbers, returns product of numbers, no side-effects
def mul(number1, number2):
	return number1 * number2

#divide 2 numbers: input 2 numbers, returns quotient of numbers, no side-effects
def div(number1, number2):
	return float(number1) / number2

#displays results: input sum, differance, product, quotient, no output, displays inputs
def display(add, sub, mul, div):
	print "The sum of the numbers is", add
	print "The differance of the numbers is", sub
	print "The product of the numbers is", mul
	print "The quotient of the numbers is", div

#asks the users for the numbers
number1 = getNumbers()
number2 = getNumbers()

#does math on inputed numbers
add = add(number1, number2)
sub = subtract(number1, number2)
mul = mul(number1, number2)
div = div(number1, number2)

#displays results of the math
display(add, sub, mul, div)
