package diceGame.diceGUI;
public class computerPlayer {
	
	//static class that takes the current score as input and returns if it is
	//less then 20
	public static boolean rollAgain(int currentScore) {
		if (currentScore < 20) {return true;}
		else {return false;}
	}
}
