/*
 * Calendar app
 * Patrick McNamara
 * 
 * Partialy functioning (the calender API is one of the worst API's of all time).
 * 
 * Will not display appointments in the proper location
 */

import javax.swing.*;

public class Main {

	public static void main(String[] args) {
		
		JPanel UI = new Interface();
		
		JFrame frame = new JFrame("Calendar");
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		
		frame.getContentPane().add(UI);
		frame.pack();
		frame.setVisible(true);
		
	}
}
